import copy
import json
from pathlib import Path


ROOT = Path(__file__).resolve().parents[1]
SRC_PATH = ROOT / "prepare_dataset.ipynb"
OUT_PATH = ROOT / "formatted_notebooks" / "01_prepare_dataset.ipynb"

STYLE = "max-width: 99%; padding:10px; border-width:3px; border-color:#efe6ef; border-style:solid; border-radius:6px"
STYLE_SUCCESS = "background-color:#e6ffe6; max-width: 99%; padding:13px; border-width:3px; border-color:#efe6ef; border-style:solid; border-radius:6px"
STYLE_WARN = "background-color:#fff8e6; max-width: 99%; padding:13px; border-width:3px; border-color:#efe6ef; border-style:solid; border-radius:6px"
STYLE_NOTE = "background-color:#f8f8fb; max-width: 99%; padding:10px; border-width:2px; border-color:#efe6ef; border-style:solid; border-radius:6px"


def source_lines(text: str) -> list[str]:
    text = text.strip("\n") + "\n"
    return text.splitlines(keepends=True)


def info(body: str) -> str:
    return f'<div class="alert alert-info" style="{STYLE}">\n\n{body.strip()}\n\n</div>'


def success(body: str) -> str:
    return f'<div class="alert alert-success" style="{STYLE_SUCCESS}">\n\n{body.strip()}\n\n</div>'


def warn(body: str) -> str:
    return f'<div class="alert alert-warning" style="{STYLE_WARN}">\n\n{body.strip()}\n\n</div>'


def note(body: str) -> str:
    return f'<div class="alert alert-secondary" style="{STYLE_NOTE}">\n\n{body.strip()}\n\n</div>'


def build_replacements() -> dict[int, str]:
    return {
        0: f"""
# <h1><center id="prep-top">Подготовка данных и синтетический причинный слой</center></h1>

### Оглавление
* <a href="#prep-load">1. Загрузка исходных данных</a>
* <a href="#prep-agg">2. Агрегация вспомогательных таблиц</a>
* <a href="#prep-preprocess">3. Предобработка набора данных</a>
* <a href="#prep-audit">4. Анализ признаков объединенного набора данных</a>
* <a href="#prep-anomalies">5. Устранение выявленных аномалий</a>
* <a href="#prep-causal-layer">6. Синтетическая генерация uplift-слоя</a>
* <a href="#prep-final">8. Итоговый синтетический набор данных</a>
* <a href="#prep-leakage">8.1 Разделение признаков и защита от утечки</a>

{info('''
<b>Задача ноутбука.</b> Превратить исходные таблицы Home Credit в воспроизводимый исследовательский набор для проверки uplift-подхода в финтехе. Сначала собирается реалистичная кредитная база, затем поверх нее строится синтетический слой коммуникаций: канал воздействия, наблюдаемый outcome, потенциальные исходы и oracle-поля.
''')}

{success('''
<b>Как читать ноутбук.</b> Кодовые ячейки формируют данные, а текстовые блоки фиксируют логику эксперимента: зачем нужен шаг, какую роль играют переменные и какие выводы стоит проверить после выполнения ячейки.
''')}

{warn('''
<b>Правило воспроизводимости.</b> Для всей работы сохраняется общий принцип проекта: случайность фиксируется через `RANDOM_SEED = 91` / `np.random.seed(91)`, а ноутбук должен выполняться сверху вниз из корня репозитория.
''')}
""",
        2: info("""
<b>Техническая подготовка.</b> Вспомогательная функция приводит многоуровневые имена колонок после агрегаций к плоскому формату. Это нужно, чтобы дальнейшие признаки имели стабильные и читаемые названия.
"""),
        4: f"""
<h2 id="prep-load">1. Загрузка исходных данных</h2>

{info('''
На этом шаге загружаются исходные таблицы Home Credit. Они дают реалистичную финансовую основу: демографию, параметры заявки, кредитную историю и признаки просрочки.
''')}

{note('''
<b>Важно для постановки.</b> В исходных данных нет истории коммуникаций с клиентами. Поэтому ниже поверх финансовых признаков создается синтетический причинный слой. Он не имитирует конкретный банк, а задает контролируемый стенд для проверки uplift-методики.
''')}
""",
        6: warn("""
<b>Фиксация случайности.</b> Эта ячейка закрепляет генератор NumPy. Без нее синтетические коммуникации, потенциальные исходы и итоговые разбиения могли бы меняться от запуска к запуску.
"""),
        8: info("""
<b>Ожидаемый вывод.</b> После чтения `application_train.csv` должна появиться размерность основной таблицы заявок. Это быстрый контроль, что сырые файлы лежат в `data/raw` и путь задан корректно.
"""),
        10: f"""
<h2 id="prep-agg">2. Агрегация вспомогательных таблиц</h2>

{info('''
Вспомогательные таблицы агрегируются до уровня клиента `SK_ID_CURR`. Так сохраняется банковская природа данных, но итоговая таблица остается пригодной для классического скоринга и последующего uplift-эксперимента.
''')}

{success('''
<b>Смысл сохраненных выводов.</b> Печатаемые размерности после каждой агрегации помогают проверить, что объединение не потеряло клиентов и что каждая таблица дала ожидаемый набор новых признаков.
''')}
""",
        17: f"""
<h2 id="prep-preprocess">3. Предобработка набора данных</h2>

{info('''
Предобработка нужна для двух целей: подготовить признаки для классического риск-скоринга и создать стабильную основу для синтетического причинного слоя. На этом этапе важно не смешивать исходные признаки клиента с будущими uplift-переменными.
''')}

{note('''
Создается отдельная копия `df_sim`: в ней можно стабилизировать пропуски для генерации синтетических переменных, не уничтожая исходную структуру пропусков в финальном датасете.
''')}
""",
        19: f"""
<h2 id="prep-audit">4. Анализ признаков объединенного набора данных</h2>

{info('''
Блок проверяет, что после агрегации набор остается пригодным для задачи кредитного риска. Сначала анализируется исходная целевая переменная `TARGET`, затем поверх нее строится отдельная задача коммуникационного воздействия.
''')}

{warn('''
<b>Как читать большие выводы.</b> Полный просмотр `df`, `df.info()` и списков колонок сохранен для аудита состава данных. Для финальной учебной версии эти ячейки можно обсудить отдельно: часть вывода можно заменить на компактные таблицы с количеством признаков по типам.
''')}
""",
        22: f"""
### Описание целевой переменной `TARGET`

{info('''
`TARGET` — исходный индикатор кредитного риска в Home Credit. Он нужен для калибровки и проверки реалистичности `BASE_PD`, но не является итоговой целевой переменной uplift-эксперимента.
''')}

{success('''
Итоговый outcome для коммуникаций задается как `TARGET_AFTER_CONTACT`: он отражает дефолт после фактически назначенного канала.
''')}
""",
        29: f"""
<h2 id="prep-anomalies">5. Устранение выявленных аномалий</h2>

{info('''
Аномалии корректируются до генерации причинного слоя. Это снижает риск, что синтетический эффект будет построен на технических выбросах, а не на содержательных финансовых различиях между клиентами.
''')}

{note('''
Блок также группирует признаки по ролям: идентификаторы, флаги, категориальные, числовые, целевые и будущие uplift-поля. Такой разбор позже помогает явно контролировать утечки.
''')}
""",
        35: f"""
<h2 id="prep-causal-layer">6. Синтетическая генерация переменных индивидуального эффекта воздействия</h2>

{info('''
Главный методический шаг ноутбука — добавление слоя коммуникаций. Он превращает задачу из обычного прогнозирования дефолта в задачу выбора воздействия: кому не писать, кому отправить SMS, кому сделать автоматический звонок, а кому нужен оператор.
''')}

{success('''
Слой специально содержит типичные сложности финтех-задач: неслучайное назначение контакта, слабый эффект, неоднородность реакции, усталость от контактов и разные каналы коммуникации.
''')}
""",
        36: f"""
### 6.1 Типы коммуникационных воздействий

{info('''
В эксперименте используются четыре варианта политики: отсутствие контакта, SMS, автоматический звонок и звонок оператора. Это минимальная многоканальная постановка, где уже возникает выбор между стоимостью канала и ожидаемым снижением риска.
''')}
""",
        37: f"""
| Значение | Описание |
|---|---|
| `control` | Отсутствие коммуникации, контрольная группа |
| `sms` | SMS-сообщение с напоминанием о платеже |
| `robot_call` | Автоматический голосовой звонок |
| `operator_call` | Звонок живого оператора контакт-центра |

{warn('''
<b>Почему нельзя сравнивать группы напрямую.</b> Назначение коммуникации не является случайным: более рисковые клиенты чаще попадают в контактные каналы, а высокорисковые клиенты чаще направляются к оператору. Это создает selection bias и делает простое сравнение долей дефолта между каналами некорректным.
''')}
""",
        38: f"""
### 6.2 Логика генерации причинного слоя

{info('''
Генерация устроена как последовательность управляемых шагов.

1. `BASE_PD` задает риск дефолта без коммуникации.
2. `CONTACT_PROPENSITY` задает вероятность того, что банк инициирует контакт.
3. `COMMUNICATION` назначает канал неслучайно: низкий риск — SMS, средний — робот, высокий — оператор.
4. `CONTACT_HISTORY`, `PREFERRED_CHANNEL`, `INTERACTION_SCORE` и `DELAY_FLAG` создают неоднородность реакции.
5. `UPLIFT_*` и `PD_*` сохраняют потенциальные эффекты и исходы для каждого канала.
6. `TARGET_AFTER_CONTACT` фиксирует наблюдаемый outcome после фактически назначенной коммуникации.
''')}

{success('''
Такой слой нужен, чтобы дальше проверять не только качество моделей, но и условия применимости uplift-подхода: наличие selection bias, гетерогенность эффекта и отличие лучшей политики от простой риск-ориентации.
''')}
""",
        39: f"""
### 6.3 Реализация функций генерации

{info('''
Функции идут в причинном порядке: сначала базовый риск, затем склонность банка к контакту, сегментация, индивидуальные модификаторы эффекта, потенциальные исходы и фактически наблюдаемый результат.
''')}
""",
        40: f"""
#### `generate_base_pd(df)` — базовая вероятность дефолта

{info('''
Функция вычисляет латентную вероятность дефолта без учета коммуникаций. Используется линейная комбинация информативных признаков: `EXT_SOURCE_1/2/3`, суммы кредита, аннуитета, дохода и признаков просрочки.
''')}

{note('''
Калибровка `mean(BASE_PD) = 0.08` выполняется бинарным поиском смещения. Это аккуратнее, чем прямой сдвиг на `logit(0.08)`, потому что среднее сигмоиды для широкого распределения не равно сигмоиде среднего.
''')}
""",
        42: f"""
#### `generate_contact_propensity(df)` — вероятность инициации контакта

{info('''
Функция моделирует банковскую политику контакта: чем выше базовый риск, сумма кредита и аннуитет, тем выше вероятность, что банк инициирует коммуникацию.
''')}

```python
CONTACT_PROPENSITY = 0.6 * BASE_PD + 0.2 * credit_norm + 0.2 * annuity_norm
```

{warn('''
Эта переменная намеренно создает смещение отбора: контактируемые клиенты систематически отличаются от контрольной группы по уровню риска.
''')}
""",
        44: f"""
#### `generate_segments(df)` — сегментация по базовому риску

{info('''
Следующая функция делит клиентов на три группы по квантилям `BASE_PD`: `low_risk`, `medium_risk`, `high_risk`. Сегмент затем используется для назначения канала и для моделирования различной реакции на один и тот же тип коммуникации.
''')}

{note('''
Фактическое назначение канала выполняется ниже в функции `assign_communication(df)`: после Bernoulli-решения о самом факте контакта низкий риск получает `sms`, средний — `robot_call`, высокий — `operator_call`.
''')}
""",
        46: f"""
#### `generate_contact_history(df)` — история контактов

{info('''
Количество предыдущих контактов моделируется из распределения Пуассона с параметром `λ = 0.5 + 3 × BASE_PD`. Более рисковые клиенты контактируются чаще, а значения ограничиваются диапазоном от 0 до 10.
''')}

{success('''
Эта переменная нужна для эффекта насыщения: повторные контакты могут ослаблять пользу коммуникации и в крайних случаях ухудшать поведение клиента.
''')}
""",
        48: f"""
#### `fatigue_multiplier(history)` — эффект насыщения контактами

{info('''
В коде насыщение реализуется как экспоненциальное ослабление эффекта: `fatigue = exp(-0.35 × CONTACT_HISTORY)`. Чем больше предыдущих контактов, тем слабее ожидаемый эффект текущей коммуникации.
''')}

{note('''
Отдельной функции `fatigue_multiplier` в коде нет: формула применяется внутри расчета эффекта канала в `calculate_channel_uplift`.
''')}
""",
        50: f"""
#### `generate_channel_preference(df)` и `generate_interactions(df)` — предпочтения и взаимодействия

{info('''
`PREFERRED_CHANNEL` задает любимый канал клиента: SMS — 50%, робот — 30%, оператор — 20%. Если назначенный канал совпадает с предпочтительным, эффект усиливается.
''')}

{note('''
`INTERACTION_SCORE` масштабирует эффект по финансовому профилю клиента: в коде используется среднее между нормированным доходом и обратной нормированной кредитной нагрузкой.
''')}
""",
        52: f"""
#### `generate_delay(df)` — отложенная реакция клиента

{info('''
Часть клиентов реагирует на коммуникацию с задержкой. Вероятность задержки задается формулой `delay_prob = 0.3 + 0.4 × BASE_PD`; при `DELAY_FLAG = 1` немедленный эффект воздействия снижается вдвое.
''')}
""",
        54: f"""
#### Потенциальные исходы и итоговая сборка

{info('''
Финальный слой хранит две группы информации. Наблюдаемая часть имитирует исторические данные: фактически назначенный канал и фактический outcome. Контрфактическая часть используется только для оценки качества и oracle-сравнений.
''')}

{warn('''
Если `UPLIFT_*`, `PD_*`, `ORACLE_*` или будущий outcome попадут в признаки модели, задача превратится в утечку. Поэтому ниже эти поля явно отделяются от безопасных признаков.
''')}
""",
        55: f"""
<h2 id="prep-final">8. Итоговый синтетический набор данных</h2>

{info('''
Итоговый набор совмещает реальные финансовые признаки и синтетический причинный слой. Он позволяет сравнивать три уровня решений: классический прогноз риска, ранжирование по ожидаемому эффекту фактически назначенной политики и идеализированный oracle-выбор лучшего канала.
''')}

{success('''
В реальном банке oracle-уровень недоступен, но в исследовательском стенде он нужен как верхняя граница и как способ проверить, восстанавливают ли модели правильный порядок клиентов.
''')}
""",
        61: f"""
<h3 id="prep-leakage">8.1 Разделение признаков по роли и защита от утечки</h3>

{info('''
После генерации все переменные разделяются по роли. Безопасные исходные признаки могут использоваться в моделях. Treatment, outcome, потенциальные исходы и oracle-поля используются только там, где это предусмотрено постановкой эксперимента.
''')}

{warn('''
Это одна из ключевых практических идей работы: uplift-модель полезна только при честном дизайне данных. Если модель видит будущий outcome или истинный эффект, качество перестает иметь прикладной смысл.
''')}
""",
    }


def build_insertions() -> dict[int, str]:
    return {
        59: f"""
{success('''
<b>Контрольная сводка после генерации.</b> Следующая ячейка показывает, как распределились клиенты по фактическим каналам, насколько различается `BASE_PD` между группами и какой средний эффект получился для каждого воздействия.
''')}

{note('''
Ожидаемая картина: `operator_call` должен концентрировать более высокий риск и более сильное среднее снижение PD; `sms` — низкий риск и меньший абсолютный эффект; контрольная группа должна иметь `TRUE_UPLIFT = 0`.
''')}
""",
        62: f"""
{success('''
<b>Финальный перенос полей.</b> Синтетические переменные переносятся обратно в основной `df`, где сохранены исходные пропуски Home Credit. Это позволяет будущим моделям работать с реалистичной структурой данных, а не с полностью медианно-импутированной копией.
''')}
""",
        65: f"""
{info('''
<b>Финальный артефакт.</b> Последняя ячейка сохраняет подготовленный датасет в `data/processed/uplift-dataset.csv`. Этот файл является входом для аналитического ноутбука, risk-baseline моделей и uplift-моделей.
''')}
""",
    }


def main() -> None:
    original = json.loads(SRC_PATH.read_text(encoding="utf-8"))
    formatted = copy.deepcopy(original)
    replacements = build_replacements()
    insertions = build_insertions()

    new_cells = []
    for index, cell in enumerate(formatted["cells"]):
        if index in replacements:
            cell = copy.deepcopy(cell)
            cell["source"] = source_lines(replacements[index])
        new_cells.append(cell)
        if index in insertions:
            new_cells.append(
                {
                    "cell_type": "markdown",
                    "metadata": {},
                    "source": source_lines(insertions[index]),
                }
            )

    formatted["cells"] = new_cells
    OUT_PATH.parent.mkdir(exist_ok=True)
    OUT_PATH.write_text(json.dumps(formatted, ensure_ascii=False, indent=1), encoding="utf-8")

    original_code = [cell for cell in original["cells"] if cell.get("cell_type") == "code"]
    formatted_code = [cell for cell in formatted["cells"] if cell.get("cell_type") == "code"]
    if original_code != formatted_code:
        raise RuntimeError("Code cells changed during formatting")

    print(f"written: {OUT_PATH}")
    print(f"original cells: {len(original['cells'])}; formatted cells: {len(formatted['cells'])}")
    print(f"code cells preserved: {len(original_code)}")


if __name__ == "__main__":
    main()
